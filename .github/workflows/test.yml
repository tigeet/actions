name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:  
      - run: cmake --version
      - run: g++ --version
      - run: g++ main.cpp -o main.exe
      - run: main.exe
